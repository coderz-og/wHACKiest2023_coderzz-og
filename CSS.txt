
body {
    font-family: Arial, sans-serif;
    text-align: center;
    background-color: #ebbbbb;
}

h1 {
    font-size: 2em;
    margin-bottom: 10px;
}

p {
    font-size: 1.2em;
    margin-bottom: 20px;
}

ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    list-style: none;
    margin: 0;
    padding: 0;
}

li {
    width: 300px;
    margin: 10px;
    background-color: #ffffff;
    box-shadow: 2px 2px 5px #CCCCCC;
    padding: 20px;
    text-align: left;
}

img {
    max-width: 100%;
    margin-bottom: 10px;
}

button {
    background-color: #dd4621;
    color: white;
    padding: 12px 20px;
    border: none;
    cursor: pointer;
    margin-top: 10px;
    width: 100%;
}

button:hover {
    background-color: #40e446;
}
/* Add a gradient background to the body */
body {
    background: linear-gradient(to bottom, #F5F5F5, #E8E8E8);
}

/* Add a border radius to the items */
li {
    border-radius: 10px;
}

/* Add a hover effect to the items */
li:hover {
    box-shadow: 4px 4px 10px #CCCCCC;
    transform: translateY(-5px);
    transition: all 0.3s ease-in-out;
}

/* Style the item heading */
h3 {
    font-size: 1.4em;
    margin-bottom: 10px;
    text-transform: uppercase;
    letter-spacing: 2px;
}

/* Style the purchase button */
button {
    font-size: 1.2em;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 2px;
    border-radius: 10px;
    transition: all 0.3s ease-in-out;
}

/* Add a hover effect to the purchase button */
button:hover {
    background-color: #2E7D32;
    transform: scale(1.1);
}

header{
    background-color: #a83636;
}

footer
{
    padding-top: 10px;
    padding-bottom: 10px;
    background-color: #ebbbbb;
    border-radius: 25px;
}